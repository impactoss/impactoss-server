<%= t(:hello).capitalize %> <%= @resource.email %>!

<%= t '.request_reset_link_msg' %>

<%= edit_password_url(@resource, reset_password_token: @token, config: @client_config.to_s, redirect_url: @redirect_url.to_s) %>

<%= t '.ignore_mail_msg' %>
<%= t '.no_changes_msg' %>></p>
